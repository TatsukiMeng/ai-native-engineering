---
title: 跨端与宿主落地：当项目不只跑在浏览器里，边界要怎么真正落代码
description: 让宿主通信、本地能力和平台差异进入可控实现，而不是停在口头设计
icon: MonitorSmartphone
---

# 这一篇先落什么边界

跨端阶段最容易出事的，不是组件样式，而是宿主通信、本地能力、权限模型、IPC 和打包差异。边界如果只停在文档里，AI 一写代码就会开始串线。

# 这一篇会把什么说清楚

这里会把桌面端、移动端和宿主层真正拉回代码实现层，先把协议和限制说清，再让跨端能力往上长，避免“看起来都通了，实际根本落不了地”。
